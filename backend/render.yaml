services:
  - type: web
    name: personal-style-backend
    env: python
    plan: free
    pythonVersion: "3.11.9"
    rootDirectory: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000